import { writable } from 'svelte/store';

export const rippleStore = writable([]);
export const imageDataStore = writable([]);
export const collageImageStore = writable([]);
export const currentImageStore = writable([]);
export const similarImageStore = writable([]);